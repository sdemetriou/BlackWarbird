using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbilityGraphZoom : MonoBehaviour
{
    private float graphSpaceScale;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Awake()
    {
      graphSpaceScale = 0.1f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}